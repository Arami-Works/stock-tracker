procs:
  api:
    cmd: doppler run --config local -- npm run dev:api
    autostart: true

  subgraph:
    cmd: doppler run --config local -- npm run dev:subgraph
    autostart: true

  router:
    cmd: doppler run --config local -- npm run dev:router
    autostart: true

  mobile:
    cmd: doppler run --config local -- npm run dev:mobile
    autostart: true

  storybook:
    cmd: doppler run --config local -- npm run dev:storybook
    autostart: false
